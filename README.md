# Init.Vim

My attempt at making a vim config. Uses Vim-Plug. File is named `init.vim` instead of
`.vimrc` as it is a neovim config, not the vim one, so it may not work with mainline vim.

Criteria:
- Small. One size only.
- Easy to understand. 
- Plugin preferred compared custom binding.
